import {
  GET_ALL_TAGS,
  GET_SELECTED_TAGS,
  SET_SELECTED_TAG,
  REMOVE_SELECTED_TAG
} from "./followingTypes";

const followingReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default followingReducer;
